name: Build Windows

# 触发：推送 v* 标签，或手动 dispatch
on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pyinstaller

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build Rust engine
        working-directory: engine
        run: cargo build --release

      # 离线引擎为纯 Python（core/pixie_dust.py），打包为单个 exe，含 PyQt6 GUI；
      # 同时把 Rust 加速引擎二进制一并打入（--add-binary），GUI 检测到即自动走快速路径。
      - name: Build GUI exe (PyInstaller)
        run: |
          pyinstaller --noconfirm --clean --onefile --windowed --name WCracking --paths . `
            --add-binary "engine/target/release/wcracking-engine.exe;." desktop/app.py
        shell: pwsh

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: WCracking-windows
          path: dist/WCracking.exe

  release:
    needs: [build-windows]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          prerelease: true
          files: |
            artifacts/WCracking-windows/WCracking.exe
